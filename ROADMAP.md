# AgentTrace 路线图

路线图描述计划，不代表功能已经实现。优先级会根据实际使用反馈调整。

## 近期

- 将 Prompt 与 Agent 版本标签连接到不可变的模板和图配置，使版本对比可复现。
- 把 `TaskStateCache` 接入运行生命周期，并增加状态一致性测试。
- 引入 Alembic，替代重建数据库的字段变更方式。
- 为离线评测输出可归档的 JSON/Markdown 报告。

## 中期

- 提供异步任务执行与受控并发，避免长运行占用 HTTP 请求。
- 增加认证、项目隔离和最小权限访问控制。
- 提供 Trace 查询与评测对比的轻量前端。
- 增加向量检索适配，同时保留确定性的关键词基线。
- 提供可嵌入其他 Agent 的追踪 SDK，而不只追踪示例 Agent。

## 暂不承诺

- 生产级高可用部署、多区域容灾和海量 Trace 存储。
- 对任意 Agent 框架的无配置自动接入。
- 真实模型质量或线上业务效果指标。

每项功能完成后，应同时更新 README 的已知限制、契约文档、测试与本路线图。
